# Prisma — Vercel / app (Accelerate URL kam direct Postgres)
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_ACCELERATE_API_KEY"

# pg_dump / pg_restore / migrations — mijokov Postgres (db.prisma.io)
DIRECT_DATABASE_URL="postgres://USER:PASSWORD@db.prisma.io:5432/postgres?sslmode=require"
